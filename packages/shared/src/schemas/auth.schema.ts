// import { z } from "zod";

// TODO: Login/register so'rov (request body) sxemalari shu yerga qo'shiladi
// Masalan: export const registerSchema = z.object({ ... });
// Masalan: export const loginSchema = z.object({ ... });
export {};